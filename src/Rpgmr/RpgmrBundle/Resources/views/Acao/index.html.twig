{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Acao list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Origem</th>
                <th>Acao</th>
                <th>Anexo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('acao_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.origem }}</td>
                <td>{{ entity.acao }}</td>
                <td>{{ entity.anexo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('acao_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('acao_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('acao_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
