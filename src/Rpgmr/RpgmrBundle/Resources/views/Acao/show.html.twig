{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Acao</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Origem</th>
                <td>{{ entity.origem }}</td>
            </tr>
            <tr>
                <th>Acao</th>
                <td>{{ entity.acao }}</td>
            </tr>
            <tr>
                <th>Anexo</th>
                <td>{{ entity.anexo }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('acao') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('acao_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('acao_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
