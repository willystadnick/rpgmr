{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Quantidade list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantidade</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('quantidade_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.quantidade }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('quantidade_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('quantidade_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('quantidade_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
